if [ -r "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc"
fi

export PS1='\[\e[33m\]\w\[\e[m\]$(bashrc_git_status)\$ '
export PROMPT_COMMAND='bashrc_check_repo; printf "\e]0;${PWD/$HOME/~}\a"'
export RECORDING=true
