#!/bin/sh

case "$1" in
  *.csv)
    csv "$1"
    ;;
  *.hs)
    HsColour "$1"
    ;;
  *.json)
    jq -C . "$1"
    ;;
  *)
    # We don't handle this format.
    exit 1
esac

# No further processing by lesspipe necessary
exit 0
